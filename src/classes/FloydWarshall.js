class FloydWarshall {
    adjacencyMatrix;
    constructor(matrix){
        this.adjacencyMatrix = matrix;
    }

    getShortestPath(startPointIdx, targetPointIdx){
        // (...)
        return [0, 1, 14, 5];
    }
}

export default FloydWarshall;